<Window x:Class="SalesSuite.Views.ReportsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
        Title="Reports" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#f4f6f8">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="?? Sales Reports"
                   FontSize="26" FontWeight="Bold"
                   Foreground="#2c3e50" Margin="0,0,0,20"/>

        <!-- Chart + Data -->
        <Grid Grid.Row="1" Margin="0,10,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="400"/>
            </Grid.ColumnDefinitions>

            <!-- Chart -->
            <Border Background="White" CornerRadius="12" Padding="15" Margin="0,0,15,0">
                <StackPanel>
                    <TextBlock Text="Monthly Sales Chart" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                    <lvc:CartesianChart x:Name="ReportsChart" Height="300"/>
                </StackPanel>
            </Border>

            <!-- DataGrid -->
            <Border Background="White" CornerRadius="12" Padding="10" Grid.Column="1">
                <StackPanel>
                    <TextBlock Text="Sales Records" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                    <DataGrid x:Name="ReportsGrid"
                              AutoGenerateColumns="True"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              IsReadOnly="True"
                              AlternatingRowBackground="#f9f9f9"
                              RowBackground="White"
                              Height="300"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Buttons -->
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="? Export PDF" Width="140" Height="40"
                    Background="#2980b9" Foreground="White"
                    Click="ExportToPdf_Click"/>
        </StackPanel>
    </Grid>
</Window>
